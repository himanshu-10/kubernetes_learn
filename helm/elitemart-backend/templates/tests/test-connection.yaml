apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elitemart-backend.fullname" . }}-test-connection"
  labels:
    {{- include "elitemart-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "elitemart-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
