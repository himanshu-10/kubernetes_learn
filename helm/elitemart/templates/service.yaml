apiVersion: v1
kind: Service
metadata:
  name: {{ include "elitemart.fullname" . }}
  labels:
    {{- include "elitemart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort:  {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "elitemart.selectorLabels" . | nindent 4 }}
